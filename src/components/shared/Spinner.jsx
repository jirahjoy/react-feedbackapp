import spinner from '../assets/spinner.gif';

const Spinner = () => {
  return (
    <img src={spinner} alt='' style={{display: 'block', margin: 'auto', width: '100px'}}/>
  )
}

export default Spinner